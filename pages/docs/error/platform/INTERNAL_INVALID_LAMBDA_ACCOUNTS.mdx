import Standard from '~/components/layout/standard'
import Note from '~/components/text/note'
import { InlineCode } from '~/components/text/code'

export const meta = {
  title: 'Internal Invalid Lambda Accounts',
  description: 'An error occurred in the platform infrastructure layer.',
  editUrl: 'pages/docs/error/platform/INTERNAL_INVALID_LAMBDA_ACCOUNTS.mdx',
  lastEdited: '2019-09-19T04:07:22.000Z'
}

#### Platform Error

An error occurred in the platform infrastructure layer.

<br />

<Note label={false}>
  Code: <InlineCode>INTERNAL_INVALID_LAMBDA_ACCOUNTS</InlineCode>
</Note>

---

#### Troubleshoot

- **[Contact Support](mailto:support@zeit.co)**.

export default ({ children }) => <Standard meta={meta}>{children}</Standard>

export const config = {
  amp: 'hybrid'
}
